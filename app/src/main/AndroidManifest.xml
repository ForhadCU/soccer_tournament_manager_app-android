<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soccertournamethelper">

    <application
        android:allowBackup="true"
        android:icon="@drawable/soccer_ic2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".SignINActivity" />
        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TourActionsActivity" />
        <activity android:name=".EntryPage3Activity" />
        <activity android:name=".TournamentsActivity" />
        <activity android:name=".PointsTableActivity" />
        <activity android:name=".EntryPage2Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>